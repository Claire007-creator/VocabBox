{
  "name": "vocabox",
  "version": "1.0.0",
  "private": true,
  "description": "VocaBox — Flashcard Learning App for IELTS, TOEFL and custom study lists",
  "scripts": {
    "build": "bash scripts/build-dist.sh",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "mobile:build": "npm run build && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/app": "^8.0.1",
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.1.0"
  }
}
